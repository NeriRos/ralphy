{
  "name": "output",
  "sourceRoot": "packages/output/src",
  "tags": ["scope:shared"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test packages/output/src",
        "cwd": "{workspaceRoot}"
      }
    },
    "test:coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test --coverage packages/output/src",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
