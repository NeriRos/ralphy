{
  "setup": [
    "bun i && make install"
  ],
  "teardown": [
    "find . -name \\\"node_modules\\\" -type d -prune -exec rm -rf '{}' +"
  ],
  "run": [
    "make install && rm -rf openspec/changes/smoke-test && rm -rf .ralph/tasks/smoke-test && bun run ralph --name \"smoke-test\" --prompt \"do nothing this is a smoke test, just do through the phases\" --claude --model haiku"
  ]
}
