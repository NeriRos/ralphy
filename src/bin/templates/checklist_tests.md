
## Checklist — Tests

Run tests for the code you wrote or modified. Fix all failures before committing.

- [ ] **Unit tests** — run tests for affected libraries/modules. If tests don't exist for new code, create them.
- [ ] **Integration tests** *(if applicable)* — run if your changes touch API routes, database queries, or cross-module boundaries.
- [ ] **E2E tests** *(if applicable)* — run if your changes affect user-facing flows (see `CLAUDE.md` for e2e command).
